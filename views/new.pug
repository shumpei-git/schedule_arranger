extends layout

block content
  form(method="post", action="/schedules")
    div
      h5 予定名(255文字まで)
      input(type="text" name="scheduleName" maxlength="255")
    div
      h5 メモ(1000文字まで)
      textarea(name="memo" maxlength="1000")
    div
      h5 候補日程 (改行して複数入力してください)
      textarea(name="candidates")
    button(type="submit") 予定をつくる